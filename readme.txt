to run the application use following steps

1. unzip the file shared
2. go to working project folder.
3.run npm install command to install the requried packages for node modules
4.run "npm start" command. it will run the application on "http://localhost:3010/"


redux is used to maintain the single object.